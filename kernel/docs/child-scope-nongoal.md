# Child / workspace scope — deferred non-goal

**BL-K-012 decision:** defer workspace/child-scope API.

SPEC-RT-001 Goal 1 requires tenancy context; it does not require nested workspace scopes for MVP. Child scope may be introduced in a later Spec/ADR if Runtime needs it.

Recorded in MODULE_STATUS.

# @dyogas/knowledge-engine

**MOD-KNOWLEDGE** · SPEC-ENGIN-002 · ADR-0006

Knowledge Source of Record (local-first). Approval-gated apply. Graph/Markdown handoff contracts only — no graph DB, no UI.

```bash
npm ci && npm test && npm run build
```

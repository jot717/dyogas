# @dyogas/research-engine

**MOD-RESEARCH** · SPEC-ENGIN-001 · ADR-0005

First user-value engine: research tasks, mock source collection, evidence, artifacts, human-approval handoff, knowledge handoff contract.

No Web UI. No SoR writes. No live network (mock adapters).

```bash
npm ci && npm test && npm run build
```

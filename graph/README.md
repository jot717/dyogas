# @dyogas/graph-engine

**MOD-GRAPH** · SPEC-ENGIN-004 · B14

GraphUpdate candidates from applied Knowledge + local hash EmbeddingJob path. In-memory only — no durable graph DB, no cloud vendor SDKs, no SoR mutation, no UI.

```bash
npm ci && npm test && npm run build
```

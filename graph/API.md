# Graph Engine API

- `proposeGraphUpdate` — GraphUpdate delta from applied Knowledge fields
- `runGraphEngine` — Runtime + Trust audit; bind `knowledge-graph-agent` + `embedding-agent`; unsealed candidates
- `buildLocalEmbeddingJob` / `LOCAL_HASH_PROFILE_ID` — deterministic local vectors (no cloud)
- `createMemoryGraphStore` — process-lifetime apply target (not a graph DB)
- `resolveOntology` / `DEFAULT_ONTOLOGY`

# @dyogas/markdown-engine

**MOD-MARKDOWN** · SPEC-ENGIN-003 · ADR-0007

Consumes Knowledge markdown handoff; produces review-ready Markdown with citations; emits unsealed `knowledge/markdown` candidates via Agent SDK. No SoR writes. No UI. No graph DB.

```bash
npm ci && npm test && npm run build
```

# Markdown Engine API

- `renderMarkdownCandidate` — handoff → review-ready body + unsealed `knowledge/markdown` candidate (Runtime + Trust + `markdown-agent` bind)
- `buildReviewReadyMarkdown` — pure Markdown body + Citations section
- `normalizeHandoff` / `MarkdownError` / `Citation` / `MarkdownHandoffInput`
